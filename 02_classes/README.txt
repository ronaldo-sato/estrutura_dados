Os arquivos desta pasta podem ser compilados por: 

(apenas passamos as implementações dos .cpp; ou seja, a implementação da
classe e a implementação da aplicação; a declaração da classe no time.h foi
incluída na implementação da classe time.cpp):

$ g++ *.cpp

Feito isso, a execução será feita com:

$ ./a.out
